import {
  actions as tActions,
  getters as tGetters,
  mutations as tMutations,
} from "./game/module";

export const gameGetters = tGetters;
export const gameActions = tActions;
export const gameMutations = tMutations;
